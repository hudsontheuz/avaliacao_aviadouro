name: Build

on:
  push:
    branches:
      - main
      - support/**
      - feature/**
      - bugfix/**

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3

      - name: compile-project
        shell: cmd
        run: .\compilador.bat

      - name: execute-test
        shell: cmd
        run: tests\AvaliacaoAviarioTests\Win64\Debug\AvaliacaoAviarioTests.exe
